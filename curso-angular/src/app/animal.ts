 export interface Animal {
    id: number // pelo fato dos meu URL conter id eu preciso colocar id aqui para criar o
    name: string
    type: string
    age: number
}

// export serve para exportar e import importar 
// interface é um conjunto de regras que deve ser seguida a risca